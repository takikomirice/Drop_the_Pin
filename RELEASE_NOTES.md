# リリースノート

## v2.2.0

写真を地図上で眺めやすくし、起動・描画の待ち時間と編集操作の安定性を改善しました。既存のスプレッドシート構成・保存データをそのまま利用できます。

### 写真と地図

- ピンを押すと写真とタイトルを吹き出しで表示。写真を押すと拡大し、右上のinfoボタンで日時・説明・タグ・リンクを写真に重ねて確認できます。
- 密集したピンを件数にまとめ、拡大すると個別表示。同じ場所のピンは放射状に展開します。ピンのタイトルを読み上げ用の名前にも設定しました。
- 上部バーから「通常地図」「陰影起伏図」を中心・縮尺を保って切り替えられます。陰影起伏図は世界版に日本の詳細図を重ね、縮尺は右下の出典表示の上に配置しました。
- 追加地図を各自のアプリに設定する案内を「使い方」に掲載しました。

### 性能と待機中の表示

- 設定・ピン・ルート・GPXを並行取得し、起動時の不要な直列待ちを減らしました。編集操作は初期読み込みの完了を待ちます。
- ピンを地図へ一括追加することで描画負荷を軽減しました。300ピンのローカル単回計測では約77msから約26msへ短縮しました（GAS通信・写真取得を含まない参考値）。
- PCにも読み込み状態とピン取得失敗時の再試行を表示します。ルート・GPXの取得失敗や一部破損を案内し、取得済みデータを維持します。

### 安定性の修正

- スプレッドシートの日時セルをタイムゾーン付きで読み取り、撮影日時の欠落を防ぐようにしました。
- ピン編集・複製・タグ一括編集で、数式として解釈される文字列を文字のまま保存します。
- ピン編集保存中の二重送信や画面切り替え、古い応答による別の編集画面への影響を防止しました。
- Drive共有エラーを組織ポリシーが原因と断定する案内を修正しました。
- GASのテンプレート条件タグが画面へ出る問題と、起動診断フラグの受け渡しを修正しました。

### 開発・検証

- GAS接続先確認、リモート退避、検証付き同期と読み戻し照合の開発コマンドを追加しました。
- 地図CSSの改行形式を固定し、Windowsで取得し直した場合も同梱データを再現できるようにしました。
- Node.jsテスト1,578件、Playwright Chromiumテスト53件、vendor・地図バンドルの同期検査が成功しました。
- テストGASで起動、写真吹き出し、地図切り替え、登録済みピン・GPXの表示を確認しました。

### 互換性と制約

- シートの列、既存データ、認可方式の移行は不要です。有料地図サービスやAPIキーの登録は追加していません。
- 陰影起伏図は日本でズーム16、世界版でズーム8を超えると元画像を拡大するため粗くなります。
- 複雑な道路ルートのキャッシュ容量対策は未対応です。通信速度の変動、スマートフォン実機での長時間利用・大量の実写真を使う検証も引き続き確認事項です。
- このGitHubリリースはGASの固定デプロイを自動更新しません。運用中の `/exec` へ適用する際は、別途GAS側のデプロイを更新してください。

## v2.1.1

### 写真表示の安定化

- 編集画面のピン写真をDriveの埋め込みURLへ直接依存せず、ピン詳細を開いたときだけ認証済みApps Script経由で取得するようにしました。
- 取得した写真をBlob URLとしてサムネイルと全画面表示で共有し、iPhone Safariを含むブラウザで安定して表示できるようにしました。
- 写真の読み込み中、取得失敗、再試行を表示し、ピン切替や詳細クローズ後の古い応答を無視してBlob URLを安全に解放します。

### スマートフォン表示

- スマートフォンでは利用できない上部の「共有」「データ」ボタンを非表示にしました。
- PCでは従来どおり共有リンク管理とデータ管理を利用できます。

### 互換性

- Spreadsheetの列、写真ファイルの保存先、写真登録フロー、音声の保存・再生仕様は変更していません。
- スマートフォンでは地図・ピン・写真・音声の閲覧を中心とし、写真ファイルをDriveへ追加したあと、PCでピンへ登録する運用を維持します。

### 検証

- Node.jsテスト: 1528件成功
- Playwright Chromiumテスト: 25件成功
- vendor再現性チェック: 成功

## v1.3.0

### 編集URLと権限管理

- configシートの `EDIT_URL` に編集用WebアプリURLを常時生成し、メニューから更新・選択できるようにしました。
- 編集キーを再生成すると `EDIT_URL` も更新され、以前の編集URLを無効化します。
- 一時編集トークンによる編集操作の保護を維持し、編集URLの共有範囲に関する案内を追加しました。

### 安定性と操作性

- スプレッドシート更新をロックとバッチ処理で安定化し、ピン・ルート・キャッシュ更新の整合性を改善しました。
- ピン詳細のDrive情報取得、ルートキャッシュ無効化、オーバーレイ操作を改善しました。

### 写真とプライバシー

- HEIC / HEIF画像をJPEGへ変換してアップロードできるようにしました。
- 写真のコピー抑止と、位置情報・共有に関する案内を強化しました。

### 検証

- ローカルの自動テストを拡充し、編集URL、認可、HEIC、シート書込み、UI操作の回帰を確認しています。
